<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:OnePieceApi.ViewModels"
             xmlns:models="clr-namespace:OnePieceApi.Models"
             x:Class="OnePieceApi.Views.SearchPage"
             x:Name="SearchRoot"
             x:DataType="vm:SearchViewModel"
             Title="Buscar">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="14">
            <Label Text="Busca un personaje"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="{StaticResource Primary}" />
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                <Entry Placeholder="Introduce un nombre"
                       Text="{Binding SearchTerm}" />
                <Button Text="Buscar"
                        Command="{Binding SearchCommand}"
                        Grid.Column="1" />
            </Grid>

            <ActivityIndicator IsRunning="{Binding IsLoading}"
                               IsVisible="{Binding IsLoading}"
                               Color="{StaticResource Primary}" />

            <Label Text="{Binding StatusMessage}"
                   TextColor="{StaticResource Primary}" />

            <CollectionView ItemsSource="{Binding Results}" SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Character">
                        <Frame Padding="0"
                               CornerRadius="14"
                               Margin="0,6"
                               HasShadow="True"
                               BackgroundColor="{AppThemeBinding Light={StaticResource LightSecondaryBackground}, Dark={StaticResource DarkSecondaryBackground}}">
                            <Grid ColumnDefinitions="120,*,Auto" RowDefinitions="Auto,Auto" Padding="10">
                                <Image Source="{Binding Image}" HeightRequest="90" WidthRequest="110" Aspect="AspectFill" Grid.RowSpan="2" Margin="0,0,10,0"/>
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" TextColor="{StaticResource Primary}" Grid.Column="1"/>
                                <Label Text="{Binding Crew}" FontSize="14" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray200}}" Grid.Column="1" Grid.Row="1"/>
                                <Label Text="{Binding Bounty}" FontSize="13" TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource Gray100}}" Grid.Column="2" Grid.RowSpan="2" VerticalOptions="Center"/>
                            </Grid>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding BindingContext.GoToDetailCommand, Source={x:Reference Name=SearchRoot}}"
                                    CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
